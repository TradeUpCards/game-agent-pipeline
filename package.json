{
  "name": "game-agent-pipeline",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm run -r build",
    "build:parser": "pnpm --filter @game-agent-pipeline/parser build",
    "dev": "pnpm run -r dev"
  }
}
